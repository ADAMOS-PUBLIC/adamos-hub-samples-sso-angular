export const environment = {
  production: true,

  // The client ID used for the SSO
  clientId: '{{CLIENT_ID}}',

  // The IAM Endpoint to athenticate against (SSO)
  iamEndpoint: 'https://id.adamos.dev/',
};
